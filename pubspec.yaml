name: bus_driver_tracker
description: A simple bus driver tracking app for university.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^11.0.0
  wakelock_plus: ^1.1.4

flutter:
  uses-material-design: true
